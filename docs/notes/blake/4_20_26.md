# Notes for Installing KinD and setting up first node 
Following the quick start guide on KinD's official website:
https://kind.sigs.k8s.io/docs/user/quick-start/
- installed kubectl

Just trying to get kind running first:
- checked go version
- `go install sigs.k8s.io/kind@v0.31.0 && kind create cluster`
    - kind command not found (?)
    - wasn't on path (added to my .zshrc)
- `kind create cluster` 
    - not working -> docker is inactive
        - systemctl start/enable docker
        - > notes for me: figure out what programs i have running on start (enable). 
        - > Change my zsh bulk delete to C-bksp, not M-bksp (futzes with a lot of other programs keybinds
        - <https://docs.docker.com/engine/install/linux-postinstall/>
        - sudo usermod -aG docker $USER (added my user to the perms)
            - `cat /etc/group | grep docker`
        - newgrp docker (activate the changes to groups)
    - got it working
- `kubectl cluster-info --context kind-kind`
    - this just tells that the control plane and CoreDNS are running (review this more later)
    - these links don't really do/tell me much
        - if you want to see them, use `kubectl proxy` instead
- `kubectl get nodes` = you can see current running nodes
- `kubectl get pods -A` = see all the pods across all the namespaces

Figuring out some docker etc:
- `docker ps` = shows all current docker containers
- `docker system df` = shows how much storage is being taken up by the containers


Building a worker node:





Data visualization + data scraping (prom and grafana)
Cluster management (Admirality and Karmada) 
Automation team
Writing and Documentation




